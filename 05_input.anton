Anton {
    // Программа спросит имя и число и выведет их
    input(name);
    input(n);

    print("Привет,", name);

    // n может быть строкой, если пользователь ввёл не число,
    // но если ввёл цифры, твой интерпретатор приведёт к int
    print("n =", n);

    if (n > 10) {
        print("n больше 10");
    } else {
        print("n не больше 10");
    }
}
