Anton {
    let s = "abc";
    let n = 3;

    print("Перед ошибкой");

    // Здесь интерпретатор должен выдать РАНТАЙМ-ОШИБКУ,
    // потому что по заданию строку умножать нельзя.
    let bad = s * n;

    print("Эта строка уже не должна выполниться");
}
